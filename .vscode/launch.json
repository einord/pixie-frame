{
    "version": "0.2.0",
    "configurations": [
        {
            "name": "Application",
            "command": "npm start",
            "request": "launch",
            "type": "node-terminal",
            "cwd": "${workspaceFolder}"
        }
    ]
}